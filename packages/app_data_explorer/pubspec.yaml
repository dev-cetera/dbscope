name: app_data_explorer
description: Interactive CLI to explore and manipulate Flutter app data on attached iOS / Android devices.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  flutter_lints: ^6.0.0
