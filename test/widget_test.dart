// Placeholder. The substantive coverage lives in the per-unit tests
// (sql_quote, envelope, cancel_token, dml, pager, server, bus_service,
// and the two param-translator suites).

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test suite loads', () {
    expect(1 + 1, 2);
  });
}
